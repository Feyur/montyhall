/**
 * Сборка «один файл» (`npm run build:offline`) — приложение открывают
 * двойным кликом с диска, а не с сайта. В ней нет аналитики и ссылок
 * на страницы сайта, чтобы файл работал полностью офлайн.
 */
export const IS_OFFLINE_BUILD = import.meta.env.MODE === 'offline';
